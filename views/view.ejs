<!DOCTYPE html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Latest compiled and minified CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css">
    <!-- jQuery library -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <!-- Popper JS -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
    <!-- Latest compiled JavaScript -->
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.0/js/bootstrap.min.js"></script>
    <title>Server Side JavaScript</title>
</head>
<body>
    <h1><a href="/topic">Server Side Javascript</a></h1>
    <table class="table table-dark">
        <thead>
          <tr>
            <th>Id</th>
            <th>Title</th>
            <th>Description</th>
            <th>Author</th>
            <th>Delete</th>
            <th>Edit</th>
          </tr>
        </thead>
        <tbody>
            <% for(i=0;i<topics.length;i++) {%>
          <tr>
            <td><%= topics[i].id %></td>
            <td><%= topics[i].title %></td>
            <td><%= topics[i].description %></td>
            <td><%= topics[i].author %></td>
            <td> <a href="/topic/delete/<%= topics[i].id %>" onclick = "return deleteConfirm()" ><button type="button" class="btn btn-danger">삭제</button></a></td>
            <td><a href="/topic/edit/<%= topics[i].id %>"><button type="button" class="btn btn-success">수정</button></a></td>
          </tr>
          <% } %>
        </tbody>
      </table>
    <% if(topic) {  %>
    <% } else {  %>
        This is Server Side JavaScript Tutorial <br>
        <a href="/topic/add"><button type="button" class="btn btn-primary">추가</button></a>
    <% } %>
</body>
    <script>
        function deleteConfirm(){
            return confirm("삭제할거야?")
        }
    </script>
</html>