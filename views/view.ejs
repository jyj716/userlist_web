<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Latest compiled and minified CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css">
    <!-- jQuery library -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>

    <!-- Popper JS -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>

    <!-- Latest compiled JavaScript -->
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.0/js/bootstrap.min.js"></script>

    <title>Server Side JavaScript</title>
</head>
<body>
    <h1><a href="/topic"> Server Side JavaScript </a></h1>
    <ol><h2>
        <% for(i=0 ; i<topics.length; i++){%>
        <a href = "/toipc/<%= topics[i].id %>">
        <li><%= topics[i].title %></a> &nbsp;:&nbsp; <%= topics[i].description %></li>
        <% } %>

    <% if(topic){ %>
    <p><%= topic.title %></p>
    <p><%= topic.description %></p>
    <p>by<%= topic.author %></p><br>
    <a href = "/topic/delete"><button type="button" class="btn btn-danger">삭제</button></a>
    <a href = "/topic/edit"><button type="button" class="btn btn-warning">수정</button></a>
     <% } else{ %></h2>
        <h2>Welcome</h2>
        This is Server Side JavaScript Tutorial
        <% } %>
    </ol>
    <a href = "/topic/add"><button type="button" class="btn btn-primary">추가</button></a>
    
</body>
</html>